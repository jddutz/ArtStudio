<UserControl x:Class="ArtStudio.WPF.Views.WorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{StaticResource ArtStudio.Surface}"
                BorderBrush="{StaticResource ArtStudio.Border}"
                BorderThickness="0,0,0,1"
                Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Workspaces"
                           Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                           VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                        Style="{StaticResource MaterialDesignIconButton}"
                        Command="{Binding RefreshCommand}"
                        ToolTip="Refresh Workspaces">
                    <materialDesign:PackIcon Kind="Refresh"/>
                </Button>
            </Grid>
        </Border>

        <!-- Workspace List -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="8">

                <!-- Loading Indicator -->
                <ProgressBar IsIndeterminate="True"
                             Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                             Margin="0,0,0,8"/>

                <!-- Built-in Workspaces -->
                <Expander Header="Built-in Workspaces"
                          IsExpanded="True"
                          Style="{StaticResource MaterialDesignExpander}"
                          Margin="0,0,0,8">
                    <ItemsControl ItemsSource="{Binding BuiltInWorkspaces}"
                                  Margin="0,8,0,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource ArtStudio.Surface}"
                                        BorderBrush="{StaticResource ArtStudio.Border}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Margin="0,0,0,4"
                                        Padding="12">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <materialDesign:PackIcon Grid.Column="0"
                                                                 Kind="{Binding IconResource, TargetNullValue=ViewDashboard}"
                                                                 VerticalAlignment="Center"
                                                                 Margin="0,0,8,0"/>

                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Name}"
                                                       FontWeight="Medium"
                                                       Foreground="{StaticResource ArtStudio.OnSurface}"/>
                                            <TextBlock Text="{Binding Description}"
                                                       Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                       Foreground="{StaticResource ArtStudio.OnSurfaceVariant}"
                                                       TextWrapping="Wrap"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2"
                                                    Orientation="Horizontal">
                                            <Button Style="{StaticResource MaterialDesignIconButton}"
                                                    Command="{Binding DataContext.SwitchWorkspaceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    ToolTip="Switch to Workspace">
                                                <materialDesign:PackIcon Kind="Play"/>
                                            </Button>

                                            <Button Style="{StaticResource MaterialDesignIconButton}"
                                                    Command="{Binding DataContext.DuplicateWorkspaceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    ToolTip="Duplicate Workspace">
                                                <materialDesign:PackIcon Kind="ContentCopy"/>
                                            </Button>

                                            <Button Style="{StaticResource MaterialDesignIconButton}"
                                                    Command="{Binding DataContext.ResetWorkspaceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    ToolTip="Reset to Default">
                                                <materialDesign:PackIcon Kind="Restore"/>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Expander>

                <!-- Custom Workspaces -->
                <Expander Header="Custom Workspaces"
                          IsExpanded="True"
                          Style="{StaticResource MaterialDesignExpander}"
                          Margin="0,0,0,8">
                    <StackPanel Margin="0,8,0,0">
                        <ItemsControl ItemsSource="{Binding CustomWorkspaces}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource ArtStudio.Surface}"
                                            BorderBrush="{StaticResource ArtStudio.Border}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Margin="0,0,0,4"
                                            Padding="12">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <materialDesign:PackIcon Grid.Column="0"
                                                                     Kind="{Binding IconResource, TargetNullValue=ViewDashboard}"
                                                                     VerticalAlignment="Center"
                                                                     Margin="0,0,8,0"/>

                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding Name}"
                                                           FontWeight="Medium"
                                                           Foreground="{StaticResource ArtStudio.OnSurface}"/>
                                                <TextBlock Text="{Binding Description}"
                                                           Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                           Foreground="{StaticResource ArtStudio.OnSurfaceVariant}"
                                                           TextWrapping="Wrap"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="2"
                                                        Orientation="Horizontal">
                                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                                        Command="{Binding DataContext.SwitchWorkspaceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip="Switch to Workspace">
                                                    <materialDesign:PackIcon Kind="Play"/>
                                                </Button>

                                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                                        Command="{Binding DataContext.DuplicateWorkspaceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip="Duplicate Workspace">
                                                    <materialDesign:PackIcon Kind="ContentCopy"/>
                                                </Button>

                                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                                        Command="{Binding DataContext.DeleteWorkspaceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip="Delete Workspace">
                                                    <materialDesign:PackIcon Kind="Delete"/>
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Add New Workspace Button -->
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Margin="0,8,0,0"
                                HorizontalAlignment="Stretch"
                                Click="AddWorkspaceButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Plus"
                                                         Margin="0,0,8,0"/>
                                <TextBlock Text="Create New Workspace"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Expander>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2"
                Background="{StaticResource ArtStudio.Surface}"
                BorderBrush="{StaticResource ArtStudio.Border}"
                BorderThickness="0,1,0,0"
                Padding="12">
            <StackPanel>
                <TextBlock Text="Current Workspace:"
                           Style="{StaticResource MaterialDesignCaptionTextBlock}"
                           Foreground="{StaticResource ArtStudio.OnSurfaceVariant}"/>
                <TextBlock Text="{Binding ActiveWorkspace.Name, TargetNullValue=None}"
                           FontWeight="Medium"
                           Foreground="{StaticResource ArtStudio.OnSurface}"/>

                <Button Style="{StaticResource MaterialDesignFlatButton}"
                        Command="{Binding UpdateCurrentWorkspaceCommand}"
                        Margin="0,8,0,0"
                        HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ContentSave"
                                                 Margin="0,0,4,0"/>
                        <TextBlock Text="Update Current"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
